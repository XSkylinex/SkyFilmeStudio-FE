export interface SceneCueValues {
  readonly musicCueId: string;
  readonly startOffsetMs: string;
  readonly gainDb: string;
  readonly loop: boolean;
  readonly fadeInMs: string;
  readonly fadeOutMs: string;
}
